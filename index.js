
      
      function Objects(object, key, value){
      
      var objectFood = {Food: 'recipe'}
      }
      
      function updateObjectWithKeyAndValue(object, key, value){
        var obj = {prop: 1}
        var newObj = Object.assign({},obj)
        return newObj
      }